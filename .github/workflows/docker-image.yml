name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image latest
      run: docker build . --file app/Dockerfile --tag opentelemtry-trace-emitter:latest
